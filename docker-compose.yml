services:
  webapi:
    image: ${IMAGE_NAME}:${IMAGE_TAG:-latest}
    container_name: demo-app
    ports:
      - "8081:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    restart: always
