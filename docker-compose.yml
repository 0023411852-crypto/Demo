services:
  webapi:
    image: ${IMAGE_NAME}:${IMAGE_TAG:-latest}
    container_name: demo-app
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    restart: always
